apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: checklist-pvc
  namespace: checklists
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: manual
  resources:
    requests:
      storage: 1Gi
